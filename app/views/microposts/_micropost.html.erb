<li>
  <span class= "timestamp">
    Date: <%= micropost.created_at %>.
  </span>
  <span class="content"><%= micropost.content %></span>
  <% if current_user.admin? %>
  <%= link_to "delete", micropost, method: :delete, data: { confirm: "You sure?"}, title: micropost.content %>
  <% end %>
</li>
